import React from "react";
import App from "./App";
export default function LeftBlue(props){
    return (
        <button type = "button" onClick = {props.blue}>turn this one blue as well</button>
    )
}//random color is broken
//name badge is broken
//the styling on the meme badge is broken
//everything else works yay
//definitely recheck to make sure everything is clean
//im going to bed for now tho because it is 8 am lol