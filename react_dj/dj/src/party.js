import React from "react";
import App from "./App";
export default function PartyTime(props) {
    return (
        <button type =  "button" onClick = {props.party}>GET READY TO PARTY</button>
    )
}